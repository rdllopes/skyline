package com.dojo.silhouette.silhouette

case class ElemSilhouette(
                         x: Int, // coordenada horizontal
                         h: Int // altura
                         ) {
  def this(x: Int) = this(x, 0)

}
